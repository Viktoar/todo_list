<!-- <h1><%= edit_list_task_path(@task.list, @task) %></h1> -->
<div class="task_edit">
	<%= form_with model: [:list, @task] do |f| %>
	<%= f.hidden_field :list_id, value: @task.list.id %>
	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.submit "update task"%>
	<% end %>

</div>